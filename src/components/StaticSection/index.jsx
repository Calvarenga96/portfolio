import { Box } from "@chakra-ui/react";
import GroguSection from "../GroguSection";

const StaticSection = () => {
  return (
    <Box mt="65px">
      <GroguSection />
    </Box>
  );
};

export default StaticSection;
